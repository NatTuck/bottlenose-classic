<% @page_title = "Bulk Add Students" %>

<h2>Upload CSV</h2>

<p>The first two columns should be Name and Email.</p>

<%= form_tag(course_path(@course) + '/bulk_add', multipart: true) do %>
  <div class="form-group">
    <%= file_field_tag :csv %>
  </div>
  <div class="form-group">
    <%= submit_tag "Upload", class: 'btn btn-primary' %>
  </div>
<% end %>

<h2>Paste A List of Emails</h2>

<p>Paste in a list of email addresses separated by spaces, newlines, commas, or
semicolons. Emails in brackets or quotes or that include names will not be handled
correctly.</p>

<p>This will request students to set their own names.</p>

<%= form_tag(course_path(@course) + '/bulk_add') do %>
  <div class="form-group">
    <%= text_area_tag :emails, nil, rows: 10, cols: 40 %>
  </div>
  <div class="form-group">
    <%= submit_tag "Add Students", class: 'btn btn-primary' %>
  </div>
<% end %>
