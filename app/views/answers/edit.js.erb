
<% if @saved %>

  $('#ask-question-<%= @question.id %>').replaceWith('<%= j render "answers/form" %>');

<% else %>

  var messages = JSON.stringify(<%= raw @errors %>);
  alert("Error saving answer: " + messages);

<% end %>

bottlenose_theme_forms();