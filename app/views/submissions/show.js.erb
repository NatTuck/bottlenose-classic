<%  @submission.team.users.each do |uu| %>
  $('#sub-for-user-<%= uu.id %>').html("<%=
    escape_javascript render('submissions/row_form', sub: @submission, user: uu )
  %>");
<% end %>

bottlenose_theme_forms();
