<%= @page_title = "Edit Assignment" %>

<p>
  <%= link_to "Back to Course: #{@course.name}", @course %>
<% unless @chapter.nil? %>
  | <%= link_to "Back to Chapter: #{@chapter.name}", @chapter %>
<% end %>
  | <%= link_to "View Assignment", @assignment %>
</p>

<%= render 'form' %>
